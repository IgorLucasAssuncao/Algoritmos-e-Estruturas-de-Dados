/*using System;

namespace Teorica1
{

    public class Teorica1
    {

        public static void Main(string[] args)
        {
            /*7
                        int segundos;
                        int horas, segundos_restantes, minutos;

                        Console.WriteLine("Insira o número de segundos: ");
                        segundos = int.Parse(Console.ReadLine() ?? "0");

                        horas = segundos / 3600;
                        minutos = (segundos % 3600) / 60;
                        segundos_restantes = (segundos % 3600) % 60;

                        Console.WriteLine($"São {horas} hora, {minutos} minutos e {segundos_restantes} segundos");
                        Console.ReadLine();
            */
/*
            int N = 0;
            double maior = 0, menor = 0, valor;

            Console.WriteLine("Insira o valor de N");
            N = int.Parse(Console.ReadLine() ?? "0");

            Console.WriteLine("Insira os valores:");
            for (int x = 0; x < N; x++)
            {
                valor = double.Parse(Console.ReadLine() ?? "0");
                if (x == 0)
                {
                    maior = valor;
                    menor = valor;
                }
                if (maior < valor)
                {
                    maior = valor;
                }
                if (menor > valor)
                {
                    menor = valor;
                }

            }
            Console.WriteLine($"O maior valor é {maior}, o menor valor é {menor}. ");

int[] x = new int[10];
int[] y = new int[10];
int[] result = new int[20];
Random rd = new Random();


}
}
}
*/